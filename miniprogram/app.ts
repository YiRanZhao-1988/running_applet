/**
 * 小程序入口文件。
 * - 仅承担全局生命周期占位，便于后续接入云开发初始化等能力。
 * - 当前不编写任何业务逻辑。
 */
App<IAppOption>({
  onLaunch() {
    /**
     * 后续可在此调用 wx.cloud.init（需先开通云开发并在 project.config / 权限中配置）。
     * 详见：https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html
     */
  },

  onShow() {},

  onHide() {},
});
