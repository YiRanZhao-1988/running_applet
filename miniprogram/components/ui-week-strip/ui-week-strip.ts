/**
 * 周日历条：仅展示星期刻度与占位节点，不代表真实日期或选中态。
 * 后续接入数据时再绑定具体周起止日与状态样式。
 */
Component({
  options: {
    addGlobalClass: true,
  },
  data: {
    /** 静态星期标签：结构占位，非训练数据 */
    dowLabels: ['日', '一', '二', '三', '四', '五', '六'],
  },
});
