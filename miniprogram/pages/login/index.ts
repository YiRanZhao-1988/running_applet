/**
 * 登录页骨架（非 Tab）。
 * 后续可配置微信登录或手机号等流程；当前无交互与校验。
 */
import { createPageOptions } from "../../utils/page-options";

Page(
  createPageOptions({
    data: {},

    onLoad() {},

    onShow() {},

    onReady() {},

    onUnload() {},
  }),
);
